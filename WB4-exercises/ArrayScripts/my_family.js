"use strict";

let myFamily = ["E,J,Toma,Tokezia"];
let numMyFamily = myFamily.length;
for (let i = 0; i < numMyFamily; i++) {
  console.log(myFamily[i]);
}

// not sure why it won't output to list form
